# V33 Raster Asset Recovery

The first V33 binary commit was malformed. The replacement pass uploads exact binary bytes and then verifies that the repository files retain valid RIFF/WebP or JPEG signatures before the preview is considered ready.
